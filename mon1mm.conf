# mon1mm sample configuration
# INI-style: key = value. Lines starting with # or ; are comments.

# UDP port to listen on for N1MM Logger+ broadcasts
udp_port = 12060

# MySQL connection settings
db_host = localhost
db_port = 3306
db_user = n1mmuser
db_password = p455w0Rd!
db_name = n1mmlogs

# The following values are optional:

# LoTW autosync interval, in seconds. (0=disabled)
lotw_sync_interval = 3600

# Path to my LoTW certificate (only required if lotw_sync_interval is >0)
lotw_certificate = mycall.p12
lotw_password = LoTWp455w0Rd!

